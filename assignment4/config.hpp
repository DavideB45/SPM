#if !defined(CONF_HPP)
#define CONF_HPP

// DEscription of the problem
static unsigned long ARRAY_SIZE = 10000000;   // Default size of the array
static unsigned long RECORD_PAYLOAD = 64;    // Default size of the record payload
static unsigned long NUM_THREADS = 4;        // Default number of threads
static long TASK_SIZE = 10000;      // Default task size for parallel processing
#endif