#if !defined(CONF_HPP)
#define CONF_HPP

// DEscription of the problem
static unsigned int ARRAY_SIZE = 1000000;   // Default size of the array
static unsigned int RECORD_PAYLOAD = 64;    // Default size of the record payload
static unsigned int NUM_THREADS = 4;        // Default number of threads

#endif